header.main-header.container(role="banner")
  .row
    a.logotype.grid-s-24.grid-m-8(href='<?php echo esc_url( home_url( "/" ))?>')
      svg.logotype__logo
        use(xlink:href='#logotyp')

    a.silhouette.grid-s-15.grid-s-offset-1(href='<?php echo esc_url( home_url( "/" ))?>')
      svg.silhouette__logo
        use(xlink:href='#silhouette')

    nav.main-nav.grid-s-24(role="navigation")
      - $menu_name = 'main_menu'; //menu slug
      - $locations = get_nav_menu_locations();
      - $menu = wp_get_nav_menu_object( $locations[ $menu_name ] );
      - $menuitems = wp_get_nav_menu_items( $menu->term_id, array( 'order' => 'DESC' ) );
      ul.main-nav__items
        - foreach($menuitems as $item):
          li.main-nav__item.button
            svg.button__background
              use(xlink:href='#shape-<?php echo rand(1, 3) ?>')
            a.link.button__link(href='<?php echo $item->url ?>')
              - if ($item->post_name == 'domu'):
                svg.button__icon
                  use(xlink:href='#home')
              - else:
                - echo $item->title
              - endif;
        - endforeach

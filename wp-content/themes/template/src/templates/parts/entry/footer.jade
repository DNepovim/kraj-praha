footer.entry-footer
    span.category-links
        - the_category()
    span.tag-links
        - the_tags()
    - if (comments_open()):
        span.meta-sep!='|'
        span.comments-link
            a(href=get_comments_link())='Comments'
    - endif
